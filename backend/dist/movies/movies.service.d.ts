import { OnModuleInit } from "@nestjs/common";
import { Cache } from "cache-manager";
import { Category, Movie, Episode } from "./movies.types";
import { PlatformKey } from "./platform-state";
export declare class MoviesService implements OnModuleInit {
    private cacheManager;
    private readonly logger;
    private readonly baseUrl;
    private readonly fallbackBaseUrls;
    private readonly imageBaseUrl;
    private readonly language;
    private region;
    private readonly readToken;
    private readonly apiKey;
    private readonly pagesPerRail;
    private readonly itemsPerRail;
    private readonly requestTimeoutMs;
    private readonly refreshRetryMs;
    private readonly genres;
    private lastCatalogError;
    private readonly seasonEpisodesCache;
    private readonly state;
    private encodeUrl;
    private providerMap;
    constructor(cacheManager: Cache);
    onModuleInit(): Promise<void>;
    private parsePositiveInt;
    private tmdbAdapter;
    private rapidApiAdapter;
    private pushNotificationAdapter;
    private isConfigured;
    private ensureConfigured;
    private tmdb;
    private getRapidApi;
    private rapidApiChanges;
    private getPushNotification;
    private loadGenres;
    private loadRealNetflixStatus;
    private image;
    private toMovie;
    refreshCatalog(platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<void>;
    private buildDynamicRails;
    private verifyOttRelease;
    private loadCatalog;
    private populateFallbackCatalog;
    private ensureCatalog;
    private toLightweightMovie;
    getAllMovies(platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Movie[]>;
    getTop10Movies(platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Movie[]>;
    getFeaturedMovie(platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Movie[]>;
    getCategories(platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Category[]>;
    getAllAggregatedCategories(): Promise<Category[]>;
    getAllFeaturedMovies(): Promise<Movie[]>;
    private selectTrailerVideo;
    getMovieById(id: string, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Movie>;
    getSeasonEpisodes(id: string, seasonNumber: number, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Episode[]>;
    searchMovies(query: string, genre?: string, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<{
        movies: Movie[];
        actor?: any;
    }>;
    private filterGenre;
    getSimilarMovies(id: string, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<any[]>;
    getRecommendations(id: string, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<Movie[]>;
    getIntroTimings(id: string, season?: number, episode?: number, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<{
        hasIntro: boolean;
        startSeconds: number;
        endSeconds: number;
    }>;
    getExternalIds(id: string, platform?: "netflix" | "prime" | "hotstar" | "appletv" | "zee5" | "sonyliv" | "jio"): Promise<any>;
    getPersonDetails(personId: string): Promise<{
        id: any;
        name: any;
        biography: any;
        profileUrl: string;
        knownFor: any;
        birthday: any;
        placeOfBirth: any;
        credits: any;
    }>;
    getStreamUrl(id: string, serverIndex?: number, season?: number, episode?: number, platform?: PlatformKey): Promise<{
        url: string;
    }>;
}
