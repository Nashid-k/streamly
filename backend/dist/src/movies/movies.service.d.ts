import { OnModuleInit } from '@nestjs/common';
import { Category, Movie, Episode } from './movies.types';
export declare class MoviesService implements OnModuleInit {
    private readonly logger;
    private readonly baseUrl;
    private readonly fallbackBaseUrls;
    private readonly imageBaseUrl;
    private readonly language;
    private region;
    private readonly readToken;
    private readonly apiKey;
    private readonly pagesPerRail;
    private readonly itemsPerRail;
    private readonly requestTimeoutMs;
    private readonly refreshRetryMs;
    private readonly genres;
    private lastCatalogError;
    private state;
    private encodeUrl;
    private providerMap;
    onModuleInit(): Promise<void>;
    private parsePositiveInt;
    private isConfigured;
    private ensureConfigured;
    private tmdb;
    private loadGenres;
    private loadRealNetflixStatus;
    private image;
    private toMovie;
    refreshCatalog(platform?: "nflix" | "nprime" | "hotstar"): Promise<void>;
    private buildDynamicRails;
    private loadCatalog;
    private populateFallbackCatalog;
    private ensureCatalog;
    private toLightweightMovie;
    getAllMovies(platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Movie[]>;
    getTop10Movies(platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Movie[]>;
    getFeaturedMovie(platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Movie[]>;
    getCategories(platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Category[]>;
    private selectTrailerVideo;
    getMovieById(id: string, platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Movie>;
    getSeasonEpisodes(id: string, seasonNumber: number, platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Episode[]>;
    searchMovies(query: string, genre?: string, platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<{
        movies: Movie[];
        actor?: any;
    }>;
    private filterGenre;
    getSimilarMovies(id: string, platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<any[]>;
    getRecommendations(id: string, platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<Movie[]>;
    getIntroTimings(id: string, season?: number, episode?: number, platform?: 'nflix' | 'nprime' | 'hotstar'): Promise<{
        hasIntro: boolean;
        startSeconds: number;
        endSeconds: number;
    }>;
    getMagnetLink(title: string, year: string): Promise<string>;
    getPersonDetails(personId: string): Promise<{
        id: any;
        name: any;
        biography: any;
        profileUrl: string;
        knownFor: any;
        birthday: any;
        placeOfBirth: any;
        credits: any;
    }>;
}
